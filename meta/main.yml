---
galaxy_info:
  author: Mattias Jonsson
  namespace: mattias-jonsson
  description: This Ansible role configures sysctl settings by overwriting the `/etc/sysctl.conf` file and creating additional configuration files under the `/etc/sysctl.d` directory as specified by the role variables.
  min_ansible_version: 2.16.0
  role_name: sysctl
  license: MIT
  platforms:
    - name: EL
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
